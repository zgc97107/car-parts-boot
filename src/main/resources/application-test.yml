server:
  port: 8080
  servlet:
    context-path: /car
logging:
  file: system.log
  level:
    root: info
spring:
  boot:
    admin:
      client:
        username: admin
        password: admin
        url: http://localhost:10000
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/car?useUnicode=true&characterEncoding=utf-8&useSSL=false&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
    password: zhao19971107A
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      max-lifetime: 1800000
      idle-timeout: 600000
      connection-timeout: 600000
      connection-test-query: SELECT 1
      auto-commit: false
  activemq:
    broker-url: failover:(tcp://192.168.0.139:61616)
    user: user
    password: user
    pool:
      enabled: true
      max-connections: 10
  kafka:
    bootstrap-servers: 192.168.0.139:9092
    producer:
      acks: 1
      retries: 0
      batch-size: 16384
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  redis:
    host: 192.168.0.141
    port: 6380
    password: minteRedis
    database: 0
    timeout: 10000
